<!DOCTYPE html>
<html>
  <head>
    <title>RentARetro</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <nav class="navbar">
     <div class="dropdown">
        <button class="btn btn-secondary fa-solid fa-bars" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        <ul class="dropdown-menu">
          <li><a class="dropdown-item" href="#">User</a></li>
          <li><a class="dropdown-item" href="#">Games</a></li>
          <li><a class="dropdown-item" href="#">Rentals</a></li>
        </ul>
      </div>
      <div class="col-6" style="text-align: center;">
        <%= link_to root_path, class: "nav-link" do %>
          <h1> RENT-A-RETRO</h1>
        <% end %>
      </div>
      <div class="login-logout">
        <h5>
          <% if user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          <% end %>
        </h5>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <footer class="container-fluid py-4">
      <div class="container">
        <div class="row gx-5">
          <div class="col-sm">
            <h3 class="mt-2">Mailing List</h3>
            <p>Recieve the latest news updates:</p>
            <form>
              <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Email address</label>
                <div class="d-flex gap-3">
                  <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter your email here">
                  <button type="submit" class="btn btn-primary">Submit</button>
                </div>
                <div id="emailHelp" class="form-text"><i>(ex.) yann@dumpter.com</i></div>
              </div>
            </form>
          </div>
          <div class="col-sm container">
            <h3 class="mt-2">Privacy Policy</h3>
            <p>This site is just a bit of fun and thus any information enterered on to this page should be done at the users discretion, as we haven't really spent the time to make sure your data is secure.</p>
          </div>
          <div class="col-sm">
            <h3 class="mt-2">Contact Us!</h3>
            <div class="footer-links">
              <a href="#"><i class="fab fa-github"></i></a>
              <a href="#"><i class="fab fa-instagram"></i></a>
              <a href="#"><i class="fab fa-facebook"></i></a>
              <a href="#"><i class="fab fa-twitter"></i></a>
              <a href="#"><i class="fa-solid fa-envelope"></i></a>
            </div>
            <h3 class="mt-3">About</h3>
            <p>This page was created by Subtracked Inc in under two days as a test of our patience and fortitude.</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
